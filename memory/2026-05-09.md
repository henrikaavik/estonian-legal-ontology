# 2026-05-09

- Analyzed Seadusloome sync SHACL warnings against `henrikaavik/estonian-legal-ontology`. Reproduced the exact Seadusloome `main` sync result: 57 JSON-LD inputs, 1,074,017 triples, 22,503 SHACL warnings = 22,290 `estleg:requestedCluster` literal node-kind violations + 213 missing `estleg:summary`. Root cause: Seadusloome clones ontology `main` and prioritizes `krr_outputs/combined_ontology.jsonld`; current ontology fixes live on `codex/ontology-review-plan` and root `_peep.json` files, while `main` and the combined artifact remain stale.
- Same Seadusloome load logic on current `codex/ontology-review-plan` reduces warnings to 1,589, all from stale extra nodes in `combined_ontology.jsonld`: 1,582 old `requestedCluster` string literals and 7 missing summaries. Root `_peep.json` files are clean. `scripts/validate_all.py` passes because it checks only `source_ids - combined_ids` and mtime, not stale extra IDs or property equality in the combined artifact.
- Created GitHub data-quality epic #136 in `henrikaavik/estonian-legal-ontology`: zero-warning Seadusloome sync and release-grade ontology artifacts. Ordered child issues: #137 stop combined self-ingestion, #138 strict source-parity validation, #139 regenerate clean combined ontology, #140 Seadusloome-compatible zero-warning SHACL command, #141 wire gate into CI/release workflow. Linked existing #122 and #125 back to the epic.
- Implemented Epic #136 end-to-end on `codex/ontology-review-plan`. DQ-1: refactored `scripts/fix_all_issues.py::generate_combined_jsonld()` to take an explicit canonical input list (`*_peep.json` + the 3 allowlisted root JSON-LDs `controlled_vocabulary.jsonld`, `karistusseadustik_eriosa_owl.jsonld`, `tsus_osa7_138_169_owl.jsonld`); never re-ingests `combined_ontology.jsonld`; added `tests/test_fix_all_issues.py`. DQ-2: enhanced `scripts/validate_all.py::validate_combined_ontology()` to reject stale extras (combined−source−allowlist) and SHACL-sensitive field drift on `@type`/`paragrahv`/`sourceAct`/`summary`/`requestedCluster`; added 5 parity tests in `tests/test_validate_all.py`. DQ-3: regenerated `krr_outputs/combined_ontology.jsonld` (2,176 stale extras + 62 drifted provisions removed; 29,873 nodes vs prior 32,049). DQ-4: added `scripts/validate_seadusloome_sync.py` mirroring Seadusloome's converter path (combined + eelnoud/riigikohus/curia/eurlex; pyshacl `inference="none"`, distinct from bucket validator's `inference="rdfs"`; grouped report; `--report` ttl artifact; exit codes 0/1/2). DQ-5: wired new `seadusloome-zero-warning` job into `.github/workflows/validate.yml` (with failure-only artifact upload), documented canonical artifacts and 5-step release order in `README.md`, and added gate section to `docs/VALIDATION_REPORT.md`. Final state: `pytest -q` 378 passed, `validate_all.py` 21,826 files / 0 errors / 0 warnings, all 4 SHACL buckets PASS, `validate_seadusloome_sync.py` PASS (57 inputs / 1,145,038 triples / 0 warnings) — same load path that previously reported 22,503 warnings on `main` and 1,589 on this branch is now 0.
